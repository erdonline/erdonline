Project E2E_DBML {
  database_type: 'MySQL'
  Note: 'DBML导入'
}

Table users {
  id integer [pk, increment, not null, note: '主键']
  name varchar [note: '姓名']
  Note: '用户表'
}

Table posts {
  id integer [pk]
  user_id integer [ref: > users.id]
  title varchar
  Note: '帖子'
}
