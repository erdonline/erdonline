# Copy to frontend/.env for a local override (gitignored).
# Empty / omitted = same-origin paths, Umi proxy → localhost:9502 (required for E2E).
# API_URL=https://api.erdonline.com
# ERD_API_URL=https://api.erdonline.com
