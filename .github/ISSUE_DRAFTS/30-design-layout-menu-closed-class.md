# [good first] 项目菜单关闭态用 CSS class 替代内联 opacity

> **已合入**（勿再投放）：`erd-project-menu--closed`（pointer-events + opacity）。

## 验证命令

```bash
cd frontend && npx playwright test tests/e2e/project-menu.spec.ts --project=chromium -g "导出"
```
