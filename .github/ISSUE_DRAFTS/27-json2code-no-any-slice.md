# [good first] json2code.ts 入口函数参数类型收窄（一小段）

> **已合入**（勿再投放）：`getAllDataSQLByFilter` / `getFieldType` 显式参数与返回类型。

## 验证命令

```bash
cd frontend && npx playwright test tests/e2e/project-menu.spec.ts --project=chromium -g "导出 DDL"
```
