# [good first] 导出 DDL 向导步骤按钮补 aria-label

> **已合入**（勿再投放）：下一步 / 上一步 / 导出 `aria-label`；E2E 断言「下一步」。

## 验证命令

```bash
cd frontend && npx playwright test tests/e2e/project-menu.spec.ts --project=chromium -g "导出"
```
