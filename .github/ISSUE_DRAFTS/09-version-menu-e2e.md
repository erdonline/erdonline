# [good first] 设计器项目菜单「版本」入口 E2E

> **已合入**（勿再投放）：菜单跳转 `/design/table/version/all`；见 `project-menu.spec.ts`。

## 验证命令

```bash
cd frontend && npx playwright test tests/e2e/project-menu.spec.ts --project=chromium -g "版本"
```
