# [good first] 版本管理页首屏骨架替代 Loading 文案

> **已合入**（勿再投放）：页内已用 `PageSkeleton`；慢网 E2E 见 `loading.spec.ts`。

## 验证命令

```bash
cd frontend && npx playwright test tests/e2e/loading.spec.ts --project=chromium -g "版本管理"
```
