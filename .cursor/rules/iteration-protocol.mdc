---
description: 自我迭代协议：每轮迭代的选题、验证、度量、汇报纪律
alwaysApply: true
---

# 自我迭代协议

## 单轮循环

0. 打底：确认全栈可起、冒烟测试在跑，缺则先补
1. 选题：按 ROI 取当前阶段最高项，一次只做一件事
2. 实施：feature 分支 + Conventional Commits，一个 commit 一个意图，随时可 revert
3. 验证：后端 `mvn verify`；前端 `yarn build` + lint；冒烟测试；关键旅程 dogfood
4. 度量：对照指标基线（覆盖率/包体积/旅程耗时/静默失败点），确认数字变好
5. 收尾：CHANGELOG + roadmap 状态标记 + 文档蒸馏 + 向用户汇报，等确认再进下一轮

## 质量红线（每轮必守）

- 不引入新漏洞；不扩大前端 `any` 数量；不破坏 docker-compose 一键启动
- 每个修复配回归测试；每个交互改动配前后对比
- 重大技术决策必须写 ADR 到 `docs/adr/`（背景/决策/后果，简短）

## 漂移防控

- 连续两轮指标变差 = 策略错误，停下来与用户重议
- 用户随时握有方向否决权；汇报用结论+证据，不问开放式方向问题
