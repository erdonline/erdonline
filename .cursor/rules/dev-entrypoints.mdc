---
description: 前后端开发唯一入口——后端用 dev-ensure，前端永不重启；禁止旁路命令
alwaysApply: true
---

# 开发入口（强制）

模型与人工只走下列入口；禁止旁路启动/杀进程「碰运气」。

## 后端（9502）——唯一入口 `dev-ensure.sh`

进程必须挂在 **tmux 会话 `erd-be`**（会话结束杀不到）。不要用 `mvn spring-boot:run`、不要在普通 shell/`nohup`/agent 后台直接跑 `dev-restart.sh`（会话结束会被 SIGKILL）。

```bash
# 幂等：健康则秒退；不健康则 tmux 拉起
./backend/dev-ensure.sh

# 改了 Java / yml / mapper / classpath 后强制重启
./backend/dev-ensure.sh --restart

# 看最近日志（/tmp/erd-be.log）
./backend/dev-ensure.sh --logs
```

| 场景 | 动作 |
|---|---|
| 验证前 / 巡检发现 9502 不通 | `./backend/dev-ensure.sh` |
| 刚改后端代码需要生效 | `./backend/dev-ensure.sh --restart` |
| 排查启动失败 | `./backend/dev-ensure.sh --logs` |
| pom 依赖变更 | 先 `JAVA_HOME=$(/usr/libexec/java_home -v 17) mvn -q dependency:build-classpath -Dmdep.outputFile=target/cp.txt`，再 `--restart` |

**禁止**：`lsof -ti:9502 \| xargs kill -9` 后手搓启动（除非 `dev-ensure` 本身失败且你在修脚本）；禁止并行起第二个后端。

依赖：本机已装 `tmux`（`brew install tmux`）。

## 前端（8000）——强制用法

| 规则 | 说明 |
|---|---|
| **永不重启** | `yarn start` 常驻；改代码靠 UmiJS HMR（1–3s） |
| **启动** | `cd frontend && yarn start`（内部会跑 `env.local.sh` 生成 `public/env-config.js`） |
| **代理** | 开发态走 `frontend/config/proxy.ts` → `http://localhost:9502`；勿改乱指生产 |
| **构建** | `yarn build`；生产 env 用 `env.sh` / `build:prod`，勿手改 `env-config.js` 当源 |
| **E2E** | `yarn test:e2e` 或 `npx playwright test --grep "关键字"`；定位纪律见 `e2e-locators.mdc` |
| **禁止** | 为「让改动生效」而杀 8000 / 重跑 `yarn start`；禁止用 `.ant-*` 写业务 E2E |

## 一页速查

```bash
# 日常：保障后端 → 前端已在跑则不管
./backend/dev-ensure.sh
# （前端若未起）cd frontend && yarn start

# 改后端后
./backend/dev-ensure.sh --restart

# 改前端后：等待 HMR，不要重启
```

MySQL / Redis：**只用 Colima + `docker-compose up -d mysql redis`**；禁止 `brew services start mysql/redis`（与容器抢端口）。详见 `docs/development.md`。

与速度纪律的关系：本文件管**入口**；增量验证分层仍遵 `dev-loop-speed.mdc`。
