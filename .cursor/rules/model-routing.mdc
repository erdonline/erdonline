---
description: 子任务模型路由：思考用强模型，执行用便宜模型
alwaysApply: true
---

# 模型路由（子任务分工）

派发 Task 子任务时遵循**思考用强模型，执行用便宜模型**；只用下表 slug，禁止杜撰其它模型名。禁止用同一次昂贵的 thinking 调用既拍板又写大段代码——想清楚后把落地丢给便宜模型。

## 路由表

| 任务性质 | Task tool `model` 参数 |
|---|---|
| **think**：选题 / ROI 排序 / roadmap 梳理 / ADR 起草与措辞 / 技术取舍 / Vision tick 选题 / 架构评审 / 对抗性 review / 卡壳两轮根因 / 红测根因定位 | 默认 `"claude-sonnet-5-thinking-high"`；硬架构决策或卡壳 2 轮升级 `"claude-opus-5-thinking-high"` |
| **exec**：实现 / bugfix / E2E 编写与修复 / 回归验证 / commit / 文档蒸馏落地 / 清死代码 | 默认 `"composer-2.5-fast"`；前端强类型/易抖动场景换 `"gpt-5.6-sol-medium"` |

## 可用 slug

思考 / think：

- `claude-sonnet-5-thinking-high`（默认）
- `claude-opus-5-thinking-high`（升级）
- `claude-fable-5-thinking-high`
- `kimi-k3-high`

执行 / exec：

- `composer-2.5-fast`（默认）
- `cursor-grok-4.5-high-fast`
- `gpt-5.6-sol-medium`
- `gpt-5.6-terra-medium`

## 纪律

- 一个任务如同时含决策与落地，拆成两次独立 `Task` 调用（先 think 后 exec），不要合并成一次
- 决策已足够明确（如切片队列本身就是决策产出）时可跳过 think，直接 exec；不要为走流程而强行加一次无实质产出的思考调用
- 拿不准归类时按 exec 默认值处理，不要硬塞思考模型
- 用户点名模型时才用其指定的 slug；不在上表则说明不可用并给出上表
- 对用户沟通时用用户自己的叫法，不暴露内部调度细节
