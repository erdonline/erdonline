---
description: 用 Playwright 真实用户旅程走查持续发现和消灭交互摩擦，禁止只改代码不走查
alwaysApply: true
---

# Playwright UX 走查（真实旅程驱动体验迭代）

体验问题只能在真实旅程中发现，代码审查发现不了"用起来不爽"。所有页面级改动必须走查，禁止"代码改完就当完成"。

## 触发时机（必须走查）

1. 改了任何页面/组件/请求层 → 用 Playwright 走一遍**受影响旅程**
2. 每轮迭代收尾 → 走一遍**全量核心旅程**（落地→登录→home→项目列表→设计器→核心操作→退出）
3. 用户报体验问题 → 先用 Playwright 复现旅程，再动手修

## 走查方法（新用户视角）

用 Playwright MCP 真实驱动浏览器，逐步操作并**截图**，以第一次使用的用户视角记录摩擦：

| 摩擦类型 | 判据 | 真实案例 |
|---------|------|---------|
| 静默失败 | 操作后无任何反馈（成功/失败/加载） | 错误凭证登录无提示；HTTP 500 无提示 |
| 死 affordance | 看起来可点但点了没反应 | 项目卡片名不可点；`<a>` 无事件 |
| 重复反馈 | 同一错误提示多次 | 「查无此用户」弹两次 |
| 多余步骤 | 达成目标需要非必要步骤 | 建表要双击打开、关系图另切标签且不渲染 |
| 空态缺失 | 无数据时白屏/无引导 | （待查） |
| 破坏性无确认 | 删除等不可逆操作无二次确认 | 画布删表直接删 |
| 文案不清 | 报错不说明原因/怎么办 | 「操作发生错误，请联系管理员」 |

## 发现后处理（分级）

- **P0 阻断**（流程走不通/数据丢失风险）→ 当轮必修
- **P1 闹心**（能用但难受）→ 排入当前或下轮
- **P2 粗糙**（打磨项）→ 记 `docs/roadmap.md` P2 区

所有发现登记 `docs/regression-checklist.md`（改动点即测试点规则）；能自动化的沉淀为 `frontend/tests/e2e/` 用例。

## 机制

- `frontend/tests/e2e/ux-audit.spec.ts`：UX 不变量自动断言（死 affordance 回归、反馈存在性等）+ 全旅程截图存档
- 截图存 `frontend/test-results/ux-walkthrough/`，每轮走查后人工快速翻一遍
- 画布类（canvas 无 DOM）交互在 ReactFlow 迁移前以手工走查为准
