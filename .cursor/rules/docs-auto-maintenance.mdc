---
description: 文档自动维护纪律：功能实现与 docs/ 文档同轮更新，功能完成即文档可对外发布
alwaysApply: true
---

# 文档自动维护（每轮迭代必守）

## 核心承诺

功能实现完成时，对应文档必须达到**可对外发布**状态。代码与文档在同一轮迭代内完成，不允许"先写代码、后补文档"。

## 触发条件与文档归属

任何改动涉及以下之一，必须同轮更新对应文档：

| 改动类型 | 必更新文档 |
|---|---|
| 用户可见的功能/交互变化 | `docs/roadmap.md` 状态标记 + 相关使用文档 |
| 架构、技术栈、数据流变化 | `docs/architecture.md` |
| projectJSON 结构/版本变化 | `docs/data-format.md`（不存在则创建） |
| 安全模型、权限、SQL 执行策略 | `docs/security-model.md`（不存在则创建） |
| 性能预算或测量方式变化 | `docs/performance-budget.md`（不存在则创建） |
| 部署、配置、环境变量变化 | `docs/deployment.md` |
| 本地开发流程变化 | `docs/development.md` |
| 交互原则的新正反例 | `docs/design-principles.md` |
| 定位、取舍、"不做什么"变化 | `docs/vision.md`（需先与用户确认） |

## 写作标准

- 面向读者：每篇回答读者的一个问题；只写**结论与理由**，不写思考过程
- 禁止出现 AI 对话痕迹、画布草稿内容、临时 TODO
- 中文为主；命令、配置、API 必须给可复制的示例
- 每轮迭代收尾：更新 `CHANGELOG.md`，并将 `docs/roadmap.md` 状态标记推进（📋→🚧→✅）

## 画布纪律

`~/.cursor/` 下的 `.canvas.tsx` 是工作草稿，永不提交进仓库；精华一律蒸馏进 `docs/`。
