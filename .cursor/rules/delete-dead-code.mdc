---
description: 清除无效代码——见到无引用/已废弃路径可直接删（前后端均可），不必先请示
alwaysApply: true
---

# 清除无效代码（随时可触发）

见到无用代码**直接删**，前后端都适用；不必先问用户确认。与当前迭代主线并行执行，不单独开轮次拖延。

## 何时动手

- 功能已下线或 ADR 明确「不保留」（如旧 OAuth password grant、社交登录、G6 画布）
- 仅剩空壳：空实现、unused import、无路由页面、无调用 Service/Controller、注释掉整段逻辑
- 依赖只服务已删功能（可顺带从 `pom.xml` / `package.json` 移除）
- 文档/注释/代理/ignore-urls 仍指向已删路径

## 怎么删（安全边界）

1. **先搜引用再删**：全仓 grep 类名/路径/接口；零引用或仅自引用才删
2. **成套删除**：Controller + Service + Mapper/XML + 前端页面/路由/代理 + 配置放行项一起清，禁止留半截
3. **表与种子慎动**：无代码引用的表/列可不删（避免破坏自部署数据）；代码层 API 与 UI 必须删干净
4. **删后必验**：`mvn compile` 或受影响包编译；前端 HMR/相关 E2E 或 curl；改动点记入 CHANGELOG「验证点」
5. **禁止借机大重构**：只删死代码与直接连带引用，不改行为、不重命名无关符号

## 典型目标（本仓库）

- 已废弃认证：`/oauth/token`、Basic client、社交 OAuth、`/login/success`、微信绑定页
- 设计器旧栈：G6 相关仅允许 S 级止血；迁移完成后整文件删除
- 模板残留：Ant Design Pro 示例页（如未接线的 `pages/user/Login`）、仅 locales 使用的死文案可后置
- JSON：禁止 fastjson 新用法；存量清零后删依赖

## 触发语（用户或自检）

「清死代码」「删无用代码」「继续推进」或审查中发现死引用 → 立即按本规则执行，无需等待单独授权。
