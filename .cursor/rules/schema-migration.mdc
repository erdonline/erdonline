---
description: Schema 增量只写 Flyway；db/init 仅 schema；单一业务库 erd
alwaysApply: true
---

# Schema 迁移（Flyway 单写）

长期策略：**增量 schema 与种子只写 Flyway**；`db/init` 仅空卷 CREATE DATABASE + CREATE TABLE（ADR-0020）。

## 真相源

| 路径 | 职责 |
|---|---|
| `backend/src/main/resources/db/migration/erd/`（`V*__*.sql`） | **增量 schema + 种子**唯一真相源；`ErdFlywayConfig` → 单一业务库 |
| `db/init/01_create_database.sql` + `02_tables.sql` | MySQL **空 data 卷**首启 bootstrap；已有库不自动重跑 |

## 强制纪律

1. **新 schema / 种子 → 只加 Flyway `V*__*.sql`**，不要往 `db/init/` 加编号补丁或 INSERT
2. **`db/init` 冻结为 schema-only**：禁止再塞 privileges / demo / e2e SQL
3. **验证**：改迁移后 `./backend/dev-ensure.sh --restart`；确认列/表已存在
4. **文档**：涉及部署/开发流程时同步 `docs/development.md` / `docs/deployment.md`

## 单一业务库

- Boot 默认 Flyway **关闭**（`spring.flyway.enabled=false`）
- 仅 `ErdFlywayConfig` 绑定 `erdDataSource`（与系统 DS 同 `DB_NAME`）
- 过渡期仍保留两套 SqlSessionFactory（按 mapper 包），见 ADR-0020

## 正反例

```text
✅  backend/.../db/migration/erd/V7__foo_add_bar.sql
✅  空卷首启依赖 db/init/02_tables.sql（终态列写进基线表定义）
❌  常规功能再加 db/init/10_whatever.sql
❌  只改 db/init、不写 Flyway（已有部署永远升不上）
```
