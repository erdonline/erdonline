---
description: 验证通过后必须自动 git commit 打节点；未验证不得宣称完成也不得提交
alwaysApply: true
---

# 验证通过即打提交节点

切片做完且**验证通过**后，必须立刻 `git commit` 保存可回滚节点，不必再问用户「要不要提交」。

## 何时必须提交

同时满足才提交：

1. 本切片改动点已按 `change-points-as-tests` 验证（curl / 单测 / 相关 E2E / 清单项）
2. 验证命令在本机实际跑过且通过（写进 CHANGELOG「验证点」）
3. 工作区有与本切片相关的未提交变更

## 提交纪律

- Conventional Commits；一个意图一个 commit（大切片可拆 2–3 个，勿混无关 WIP）
- 遵循仓库既有 commit 流程（heredoc、不改 git config、不 `--no-verify`、不 amend 他人提交）
- 禁止提交密钥、`test-results/`、本地 canvas、`.env*`
- 提交说明写清「为什么」+ 验证摘要（如 `version.spec 绿`）

## 何时不提交

- 验证失败或未跑完
- 仅调研/读代码无实质改动
- 用户明确说「先别提交」

## 与其它规则

- 未验证不得宣称完成（`change-points-as-tests`）
- 清死代码、文档同轮完成后再打点（`delete-dead-code` / `docs-auto-maintenance`）
