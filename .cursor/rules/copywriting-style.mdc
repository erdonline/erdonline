# 文案风格（Copywriting Style）

> 面向所有贡献者：写任何**用户可见**文案（UI、页面、按钮、错误、空态、README、文档、落地页、邮件、推送）之前必读。
> 目标：提升注册率、Demo 完成率、版本保存数、Agent 使用率。所有文案改动必须遵守本文。

## 一句话定位（所有文案的锚点）

**中文**：数据库设计的 Git + Figma。版本 + 协作是壁垒，开放 `projectJSON` 让人和 Agent 读写同一份事实源。

**英文**：Git + Figma for database design. Versions + collaboration are the moat; open `projectJSON` is the source of truth humans and agents read/write.

任何文案先问：它是否强化这个定位？不强化就删。

## 语气（Tone）

- **技术精确**：用 `projectJSON`、`version`、`diff`、`PAT`、`MCP`、`ERD`，不写「那个东西」「智能数据库」「一键生成」。
- **诚实克制**：不夸大，不承诺做不到的事。说「读写同一份 projectJSON」，不说「AI 自动帮你建模」。
- **行动导向**：每条文案回答用户「我现在该做什么」。CTA 用动词，不写「了解更多」，写「打开 Demo」。
- **分层沟通**：首次用户给 30 秒路径；深度用户给幂等操作；贡献者给技术细节。不混在一起。
- **不恭维用户**：不写「你真棒」「恭喜」，写「已保存」「已创建版本 v1.2」。

## 必守写法（MUST）

1. **动词开头**：按钮/链接/标题用动词。
   - ✅ 「打开 Demo」「铸造 PAT」「粘贴到 Cursor」「建议一版」
   - ❌ 「了解 Demo」「PAT 管理」「AI 集成」「版本建议」

2. **具体数字 + 具体动作**：不写「快速」「轻松」，写「30 秒」「一次保存」「一行 JSON」。
   - ✅ 「30 秒免注册试用：打开 demo → 改一张表 → 存一个版本 → 看一次 diff」
   - ❌ 「快速上手，轻松体验」

3. **反事实/边界清晰**：告诉用户「这不是什么」，降低错误预期。
   - ✅ 「不是 ChatSQL」「不会凭一句话生成新图」「API 200 不是人批准」「Demo 分享链接不是 PAT」
   - ❌ 「我们支持 AI」「MCP 集成强大」

4. **先给路径，再给解释**：用户先知道怎么做，再知道为什么。
   - ✅ 「复制下面 JSON → 粘进 `~/.cursor/mcp.json` → 把 `erd_pat_…` 换成你的明文 PAT」
   - ❌ 「下面将介绍如何把 ERD Online 与 Cursor 集成……」

5. **术语一致**：全站统一使用下列术语，不混用。
   - `projectJSON`（不写「项目 JSON」「JSON 模型」）
   - `版本` / `version`（不写「快照」「历史」）
   - `diff`（不写「差异比对」「变更记录」）
   - `Agent` / `AI Agent`（不写「AI」「机器人」「助手」当主语）
   - `MCP`（不写「模型上下文协议」，除非首次定义时括注）
   - `PAT` / `Personal Access Token`（不写「令牌」单独出现，除非上下文已定义）
   - `ERD Online`（不写「ERDOnline」「ERD Online 平台」）

6. **错误/空态 = 解释原因 + 下一步动作**：不沉默、不责怪、不吓唬。
   - ✅ 「项目列表为空——先在建模器里创建你的第一个项目，再回到这里让 Agent 列出项目。」
   - ✅ 「401：PAT 已过期或权限不足。请重新铸造。」
   - ❌ 「出错了」「请联系管理员」「无数据」

7. **CTA 单一路径**：同一屏只允许一个主 CTA。次 CTA 必须明显是次要。
   - 落地页：主 CTA =「打开在线 Demo」；次 CTA =「浏览模板」/「自部署」；已登录时主 CTA =「进入工作台」。
   - `cursor-mcp` 页面：主 CTA =「一键在 Cursor 中打开」；次 CTA =「复制 mcp.json」「完整文档」。
   - PAT 弹层：主 CTA =「复制 mcp.json」；次 CTA =「一键在 Cursor 中打开」。

8. **Demo 优先于注册**：新用户先看到真实关系图，再要求注册。文案必须体现。
   - ✅ 「先看图，再决定注册」
   - ❌ 「立即注册，体验强大功能」

9. **中英文同信息密度**：英文不是中文的字面翻译；同一句话给同样多的信息。
   - 中文「人和 AI agent 共用同一份数据结构」≈ 英文「humans and AI agents share the same data structure」。
   - 不要中文写 30 字、英文写 10 词。

10. **GitHub README 是获客页**：README 的 MCP 章节必须包含：PAT 铸造入口、可复制 mcp.json、支持的客户端列表、GitHub Release tarball 路径、以及「这不是 ChatSQL」边界说明。

## 禁忌（NEVER）

- 不写「一键生成 ER 图」「AI 自动建模」「智能推荐 schema」—— 这是 ChatSQL 生态位，我们不打。
- 不写「革命性」「颠覆性」「极致体验」。
- 不写「我们很高兴地宣布」「我们很自豪」。
- 不写「点击这里」「点我」。
- 不写「您好」「亲爱的用户」。
- 不写「系统繁忙」「网络错误」这种无信息错误；必须给出用户可执行的动作。
- 不在同一屏堆超过 3 个 CTA。
- 不在 README/落地页用「平台」代替「工具」或「项目」除非强调事实源。
- 不把「PAT」和「分享链接」混为一谈。

## 场景模板

### 落地页 Hero
- Kicker：`开源 · MIT`
- Title：`数据库设计的 Git + Figma`
- Lead：`在线绘制 ER 图：版本、协作、开放格式——人和 AI agent 共用同一份数据结构。30 秒免注册试用。`
- CTA（未登录）：`打开在线 Demo` / `浏览模板广场`
- CTA（已登录）：`进入工作台` / `打开演示`

### Demo 引导（新用户）
- 「打开在线 Demo → 你会看到用户/订单表的关系图。改一张表，存一个版本，看一次 diff。」
- 「不是 ChatSQL；你改的是设计器同一份 projectJSON。」

### 注册/登录页
- 标题：「注册后即可保存、分享、协作」
- 副标题：「无需信用卡。可先打开 Demo 看图。」
- CTA：「注册」/「已有账号？登录」

### PAT 弹层
- 标题：「铸造 Personal Access Token」
- 说明：「明文只显示一次。复制下面 mcp.json 后，把 `erd_pat_…` 换成这串明文。」
- 主 CTA：「复制 mcp.json」
- 次 CTA：「一键在 Cursor 中打开」

### MCP / cursor-mcp 页面
- 三步结构：铸造 PAT → 粘贴配置 → 对 Agent 说「列出我的 ERD 项目」
- 客户端列表：Cursor、Claude Desktop、Claude Code、Cline/Roo、Windsurf、Glama、其它
- 安全提示：「一键链接不会把 PAT 编进 URL」「写操作需要人在设计器里 diff 审批」

### 错误 / 空态
- 「项目列表为空 → 先创建项目」
- 「401 → 重新铸造 PAT」
- 「网络失败 → 点击重试」

### 版本/写操作
- 「API 200 不是人批准。请在设计器里打开版本 diff 确认。」
- 「建议一版：让 Agent 调用 `create_version`，你再到设计器里 diff。」

## 度量与迭代

每轮文案整改必须登记：
- 改动点（哪个页面/按钮/段落）
- 预期指标（注册率、Demo 完成率、PAT 铸造数、MCP 接入数、版本保存数）
- 验证方式（A/B 或前后对比）
- 结果（写入 CHANGELOG / docs/growth.md）

北极星指标：**每周产生版本保存的活跃建模项目数**。文案整改必须服务于这个指标，而不是「看起来更专业」。

## 与既有规则的关系

- UI 交互遵守 `design-principles.md`
- 代码与测试遵守 `iteration-protocol.mdc` / `change-points-as-tests.mdc`
- 本文只管**用户可见文字**；改文案不改交互时，仍需 E2E 断言文案（`getByText` / `toHaveText`）或手工走查。
