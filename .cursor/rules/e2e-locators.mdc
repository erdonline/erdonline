---
description: E2E 定位优先级——role/可访问名优先，data-testid 兜底，禁止 ant 类名
alwaysApply: true
---

# E2E 定位纪律（Playwright + antd）

行业标准（Playwright / Testing Library）与 antd 组件现实对齐。写/改 E2E 或为测试补选择器时必须遵守。

## 定位优先级（高 → 低）

1. **`getByRole(role, { name })`** — 首选；按钮、链接、菜单项、选项、对话框
2. **`getByLabel` / `getByPlaceholder` / `getByText`** — 表单与可见文案
3. **`getByTestId`** — 仅当用户看不到稳定语义时（树叶子、画布容器、纯图标按钮）
4. **禁止**：`.ant-*` / 哈希类名、深层 CSS、XPath、`nth` 碰运气

## 产品侧怎么配合

- 用户可点控件：保证**可访问名**（按钮可见文案、`aria-label`、Form `label`）
- 语义天然缺失（树「关系图」、RF 空态 CTA、图标 +）：加 `aria-label` **或** `data-testid`（二选一即可，优先 aria）
- antd 透传：`data-testid` / `aria-*` 挂在触发器上；Select/DatePicker 弹层用 `getByRole('option')`，勿扫 `.ant-select-dropdown`
- Modal/Drawer：`getByRole('dialog')` 再链式 `getByRole('button', { name })`

## 正反例

```ts
// ✅
await page.getByRole('button', { name: '打开模型' }).click();
await page.getByPlaceholder('请输入项目名').fill(name);
await page.getByTestId('tree-open-relation').click(); // 树叶子无稳定 role 名时

// ❌
await page.locator('.ant-tree [class*=title]', { hasText: '关系图' }).click();
await page.locator('.ant-btn-primary').nth(2).click();
```

## 反脆弱（i18n 奠基后 · 新功能必守）

奠基切片（ADR-0023）已完成：Theme locale 经 `getAntdLocale()` 可配置，**默认仍为 zh-CN**。新功能 E2E **不得**把中文可见文案当作唯一定位锚（日后切 locale 会脆断）。

| 场景 | 做法 |
|---|---|
| **新** chip / Modal / 工具条按钮 | 先 `getByTestId` 或带稳定 `aria-label` 的 `getByRole`；**避免**新增 `getByRole('button', { name: '中文文案' })` 作为唯一定位 |
| 需断言用户可见 copy | **定位与断言分离**：`const el = page.getByTestId('…'); await expect(el).toBeVisible(); await expect(el).toHaveText(/…/)` |
| 既有 spec | grandfathered；不批量改中文 `getByRole({ name })`，除非顺带修该旅程 |

产品侧：Vision loop / 新交互控件优先补 `data-testid`（或语义 `aria-label`），见 `agent-loop-vision.prompt.md`。

## 与既有规则

- 旅程走查仍遵 `playwright-ux-audit.mdc`
- 改动点须有自动化/清单：`change-points-as-tests.mdc`
